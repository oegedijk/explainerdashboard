explainer_methods
*****************

This module contains helper functions, mostly to format data into
a convenient format so that they can be fed to the plot functions
in ``explainer_plots.py``.

*(Under construction: still need to add sphinx compatible docstrings to the functions)*

.. automodule:: explainerdashboard.explainer_methods